package Helpers;

import Helpers.MergeSort;
import Helpers.EditDistance;

public class TextHelpers {
    /*
    // Okay maybe find a better solution later
    // For bodie: this variable is all the numbers from 0-9 plus period, as a set you can work with
    public static Set<String> NUMERALS = new HashSet<String>(Arrays.asList("0123456789.".split("",0)));

    public static String[] stripExt(String filename) {
        // Use this function to take in a string with an arbitrary number of periods and return two strings as an array:
        // { name, ext }
        return namext;
    }

    public static String simplifyString(String name) {
        // Take in a string and return the string all lowercase with no punction or spaces (With the exception of _)
        return out;
    }

    public static String fixSpaces(String s) {
        // Convert all spaces in a string to _, and return the string
        return newS;
    }

    public static String[] findSimilar(String[] names, String name) {
        // given an array of strings names and a name name, find the top 7 most similar names
        // You will be given a function eDist(String A, String B, int A, int, B) that compares two strings given
        // the strings and the length of each string. It will return the difference between the strings as a number
        return topList;
    }

    public static Int[] intSort(Int[] numbers){
    }

    public static String[] stringSort(String[] strings){
    }
*/
}
